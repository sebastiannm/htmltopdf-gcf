{
  "name": "html-to-pdf",
  "version": "1.0.14",
  "engines": {
    "node": ">=14"
  },
  "scripts": {
    "deploy": "gcloud functions deploy htmltopdf --trigger-http --runtime nodejs20 --memory 1024MB --region europe-west1"
  },
  "dependencies": {
    "puppeteer": "^21.4.1"
  }
}

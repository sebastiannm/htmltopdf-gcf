{
  "name": "html-to-pdf",
  "version": "1.0.0",
  "engines": {
    "node": ">=12"
  },
  "scripts": {
    "deploy": "gcloud functions deploy htmltopdf --trigger-http --runtime nodejs12 --memory 1024MB"
  },
  "dependencies": {
    "puppeteer": "^5.2.1"
  }
}

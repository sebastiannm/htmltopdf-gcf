{
  "name": "html-to-pdf",
  "version": "1.0.0",
  "engines": {
    "node": ">=10"
  },
  "scripts": {
    "deploy": "gcloud functions deploy htmltopdf --trigger-http --runtime nodejs10 --memory 1024MB"
  },
  "dependencies": {
    "puppeteer": "^1.14.0"
  }
}
